<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NoticiaMovil.ViewModels"
             x:Class="NoticiaMovil.Views.IncioView" BackgroundColor="Orange"  NavigationPage.HasNavigationBar="False">
    <ContentPage.BindingContext>
        <vm:NoticiasViewModel x:Name="noticiasView"></vm:NoticiasViewModel>
    </ContentPage.BindingContext>
    <ScrollView>
        <RefreshView  Command="{Binding ActualizarCommand}" IsRefreshing="{Binding EstaCargando}">
            <CollectionView ItemsSource="{Binding Noticias}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>

                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Encabezado}" Padding="5" HorizontalOptions="FillAndExpand"></Label>
                            <Button Command="{Binding Source={x:Reference noticiasView}, Path=VerCommand}" 
                                    CommandParameter="{Binding .}"
                                    Text="Validar" HorizontalOptions="End"></Button>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </ScrollView>
</ContentPage>