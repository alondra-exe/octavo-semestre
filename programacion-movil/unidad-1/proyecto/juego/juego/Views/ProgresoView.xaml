<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:juego.ViewModels"
             x:Class="juego.Views.ProgresoView"
             Title="Mis récords"
             BackgroundColor="#49648A">
    <ContentPage.BindingContext>
        <vm:ProgresoViewModel></vm:ProgresoViewModel>
    </ContentPage.BindingContext>
    <ScrollView>
        <StackLayout Margin="10" BindableLayout.ItemsSource="{Binding Reportes}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Frame>
                        <Grid RowDefinitions="*,*" ColumnDefinitions="80">
                            <Label Text="{Binding Id}"></Label>
                            <Label Text="{Binding Puntuacion}"></Label>
                            <Label Grid.Column="1" Text="{Binding Intentos}"></Label>
                            <Label Grid.Column="2" Text="{Binding Fecha}"></Label>
                            <Label Grid.Column="3" Text="{Binding IdAlumno}"></Label>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </ScrollView>
</ContentPage>