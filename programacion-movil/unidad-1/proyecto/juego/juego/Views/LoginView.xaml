<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:juego.ViewModels"
             x:Class="juego.Views.LoginView"
             BackgroundColor="#171818">
    <ContentPage.BindingContext>
        <vm:AlumnoViewModel></vm:AlumnoViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Margin="30">
            <StackLayout.Resources>
                <Style TargetType="Entry">
                    <Setter Property="PlaceholderColor" Value="#367E8D"></Setter>
                    <Setter Property="TextColor" Value="White"></Setter>
                </Style>
            </StackLayout.Resources>
            <Label FontSize="30" FontAttributes="Bold" HorizontalTextAlignment="Center" TextColor="#367E8D" FontFamily="Arial" Text="Multiplica+"></Label>
            <Entry Placeholder="Correo" Text="{Binding AlumnoLogin.Correo}" x:Name="etyCorreo"></Entry>
            <Entry Placeholder="Contraseña" IsPassword="True" Text="{Binding AlumnoLogin.Contrasena}" x:Name="etyContrasena"></Entry>
            <Button Text="Iniciar sesión" Command="{Binding LoginCommand}" BackgroundColor="#367E8D" TextColor="White" FontSize="15" Margin="0,40,0,0"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>