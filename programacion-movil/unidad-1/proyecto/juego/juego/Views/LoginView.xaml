<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:juego.Models"
             x:Class="juego.Views.LoginView">
    <ContentPage.BindingContext>
        <m:Alumno></m:Alumno>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Bienvenido"></Label>
            <Entry Placeholder="Correo" Text="{Binding Correo}"></Entry>
            <Entry Placeholder="Contraseña" Text="{Binding Contrasena}"></Entry>
            <Button Text="Iniciar sesión" Command="{Binding Login}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>