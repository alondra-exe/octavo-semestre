<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:juego.ViewModels"
             x:Class="juego.Views.LoginView">
    <ContentPage.BindingContext>
        <vm:AlumnoViewModel></vm:AlumnoViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Bienvenido"></Label>
            <Entry Placeholder="Correo" Text="{Binding AlumnoLogin.Correo}" x:Name="etyCorreo"></Entry>
            <Entry Placeholder="Contraseña" Text="{Binding AlumnoLogin.Contrasena}" x:Name="etyContrasena"></Entry>
            <Button Text="Iniciar sesión" Command="{Binding LoginCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>